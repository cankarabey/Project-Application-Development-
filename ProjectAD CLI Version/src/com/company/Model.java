package com.company;

public class Model {

    public static double calcDeer(double rValue , int nZero , int t){
        double result;
        result = nZero * Math.pow(Math.E , (rValue * t));
        return result;
    }

    public static double calcHorse(double rValue , int nZero , int t){
        double result;
        result = nZero * Math.pow(Math.E , (rValue * t));
        return result;
    }

    public static double calcCattle(double rValue , int nZero , int t){
        double result;
        result = nZero * Math.pow(Math.E , (rValue * t));
        return result;
    }




}
